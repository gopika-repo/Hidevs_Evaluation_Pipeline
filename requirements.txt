# ============================================================
# Dave Evaluation Pipeline — Requirements
# ============================================================

# ------------------------------------------------------------
# Web API / Server
# ------------------------------------------------------------
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# ------------------------------------------------------------
# Data Validation
# ------------------------------------------------------------
pydantic>=2.7,<3.0

# ------------------------------------------------------------
# Evaluation Frameworks
# ------------------------------------------------------------
deepeval==4.1.5

# Only trulens.providers.google is imported anywhere in this codebase
# (evaluation_pipeline/evaluators/groundedness_evaluator.py). The old
# trulens-eval package additionally installs the Streamlit dashboard +
# full Jupyter/notebook stack (200+ packages), which was bloating the
# build and causing slow container spin-up / deploy timeouts on Render.
# Swapped for the lighter core packages that only bring in what's used.
trulens-core==2.12.0
trulens-feedback==2.12.0
trulens-providers-google==2.12.0

# ------------------------------------------------------------
# LangChain / LLM Integration
# ------------------------------------------------------------
langchain>=0.2.0
langchain-google-genai>=2.0.0

# ------------------------------------------------------------
# Google Gemini
# ------------------------------------------------------------
google-generativeai>=0.8.0

# ------------------------------------------------------------
# LLM Gateway / Providers
# ------------------------------------------------------------
litellm==1.96.0

# ------------------------------------------------------------
# Environment / Configuration
# ------------------------------------------------------------
python-dotenv>=1.0.0